# Current checkpoints

This directory contains one maintained checkpoint per active, resumable work stream. Do not put completed-task reports, design decisions, transcripts, or speculative backlog here. Create or update a file only when meaningful unfinished state remains. The work-stream owner maintains it until completion, then promotes durable outcomes and archives or removes it. Files must follow the checkpoint schema and never override durable artifacts.

There is no active checkpoint for the completed version 0.1 bootstrap.
